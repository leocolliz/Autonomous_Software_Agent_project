;; domain file: domain-domain-lights-0.pddl
(define (domain default)
    (:requirements :strips)
    (:predicates
        (switched-off ?l)
        (switched-on ?l)              
    )
    (:action LightOn
    :parameters (?l)
    :precondition (and
(switched-off ?l)
    )
    :effect (and
(switched-on ?l) (not (switched-off ?l))
    )
)
)